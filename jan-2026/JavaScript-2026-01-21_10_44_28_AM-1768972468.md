## 🧠 JavaScript Event Loop — Guess the Output

### 📌 The Code

```ts
console.log('1');

setTimeout(() => console.log('2'), 0);

Promise.resolve().then(() => console.log('3'));

console.log('4');
```

### Explanation

Output:

```js
1 4 3 2 // Explanation:
```

1. 1' is logged immediately.
2. '4' is logged immediately after '1'.
3. The Promise Promise has higher precision because they are part of micro task queue, so they will execute first.
   - Order 1st process nexttick() 2nd promise queue
4. The setTimeout Is part of macro task, an inside macrotask it is a first phase, so it will execute after promise.
   - Order 1 timer 2 I/O 3 idle, prepare 4 poll 5 check 6 close callbacks
